eth:
  outputs:
    ci:
      account: GOXVEIM-GL43375
      database: CI
      private_key_path: "{{ env_var('DBT_PRIVATE_KEY_PATH') }}"
      role: ACCOUNTADMIN
      schema: CI_SCHEMA
      threads: 1
      type: snowflake
      user: rob3fish
      warehouse: COMPUTE_WH
  target: CI